# Firebase тохиргооны заавар — KPI consulting

Сайт нь Firebase Firestore (өгөгдөл) + Firebase Authentication (админ нэвтрэлт)-той
холбогдсон. Кодын тал бүрэн бэлэн. Доорх алхмуудыг **Firebase консол дээр** хийхэд
бүрэн ажиллана.

> Консолын домэйн дээр автоматжуулалт хийх боломжгүй тул эдгээр товшилтуудыг та
> өөрөө хийнэ. Хэдхэн минутын ажил.

Бүх алхмыг https://console.firebase.google.com → **kpiconsulting** төслөөс хийнэ.

---

## Алхам 1 — Firestore Database үүсгэх

1. Зүүн цэс → **Build → Firestore Database**
2. **Create database**
3. Байршил: `asia-southeast1` (Сингапур — Монголд ойр)
4. **Start in production mode** → **Create**

## Алхам 2 — Authentication идэвхжүүлэх

1. Зүүн цэс → **Build → Authentication** → **Get started**
2. **Sign-in method** таб → **Email/Password** → **Enable** → **Save**

## Алхам 3 — Админ хэрэглэгч үүсгэх

1. Authentication → **Users** таб → **Add user**
2. Админы **и-мэйл** болон **нууц үг** оруулаад **Add user**
   *(Энэ и-мэйл/нууц үгээр `admin.html`-д нэвтэрнэ.)*
3. Үүссэн хэрэглэгчийн мөрөнд байгаа **User UID**-г хуулж ав (жишээ: `a1b2c3...`).

## Алхам 4 — Админыг зөвшөөрөгдсөн жагсаалтад нэмэх

> Энэ нь өөрийгөө бүртгэсэн танихгүй хүн админ болохоос сэргийлдэг чухал алхам.

1. **Firestore Database → Data** таб → **Start collection**
2. Collection ID: `admins`
3. **Document ID** талбарт **3-р алхамд хуулсан UID**-г тавь
4. Ямар нэг талбар нэм (жишээ: field `ok`, type `boolean`, value `true`) → **Save**

## Алхам 5 — Аюулгүй байдлын дүрэм тавих

1. **Firestore Database → Rules** таб
2. `firestore.rules` файлын **бүх агуулгыг** хуулж, байгаа дүрмийг орлуулж тавь
3. **Publish**

Болоо! `admin.html`-аа нээж, 3-р алхмын и-мэйл/нууц үгээр нэвтэрнэ үү.

---

## Өгөгдлийн бүтэц

| Firestore | Агуулга | Хандалт |
|---|---|---|
| `content/*` | Нийтлэл, темплэйт, холбоо барих, "Яагаад бид", цагийн хуваарь, асуулга | Хэн ч **унших**, зөвхөн админ **бичих** |
| `submissions/*` | Үнийн хүсэлт, цаг захиалга, бүртгэл (хувийн мэдээлэл) | Зочид зөвхөн **үүсгэх**, зөвхөн админ **унших** |
| `public/taken_slots` | Захиалагдсан цагийн жагсаалт (PII биш) | Хэн ч |
| `admins/*` | Админы UID жагсаалт | Зөвхөн консолоос |

- Админ контент засвал нийтийн сайтад **бодит цагт** тусгагдана.
- Зочны шинэ хүсэлт/захиалга админд **автоматаар** гарч ирнэ.
- Хувийн мэдээлэл (нэр/утас/и-мэйл) зөвхөн нэвтэрсэн админд харагдана.

---

## Санамж

- **Firestore үүсгэх хүртэл** нийтийн сайт localStorage горимд хэвийн ажиллана.
- **Authentication тохируулах хүртэл** админ нэвтрэх боломжгүй (алдаа зөв харагдана).
- Хэд хэдэн админ хэрэгтэй бол 3–4-р алхмыг давтаж, тус бүрийн UID-г `admins`-д нэм.
- Ашиглалтын дараа код өөрчилвөл `firebase.js?v=2` дахь дугаарыг ахиулж (v=3, …)
  browser-ийн кэшийг шинэчил.
